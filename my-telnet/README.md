# telnet

telnet は平文での通信。  


## 手順１

PowerShell ではなく、 Command Prompt を使う。  

```shell
pip install telnetlib3
```


## 手順２

```shell
cd my-telnet

python -m my_telnet --host {IPアドレス} --port {ポート番号}
```
